{
  "type": "panel",
  "title": "[% edq(lang('origin_of_ingredients')) %]",
  "elements": [
    [% FOREACH origin IN product.origins_tags %]
    {
      "type": "panel",
      "title": "[% display_taxonomy_tag('origins_tags', origin) %]"
    }[% IF !loop.last %],[% END %]
    [% END %]
  ]
},
