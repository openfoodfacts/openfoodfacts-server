version: "3.7"
services:
  memcached:
    restart: always
  postgres:
    restart: always
  backend:
    restart: always
  frontend:
    restart: always
volumes:
  icons_dist:
    external: true
  js_dist:
    external: true
  css_dist: 
    external: true
  node_modules:
    external: true
  html_data:
    external: true
  users:
    external: true
  products:
    external: true
  orgs:
    external: true
  product_images:
    external: true
