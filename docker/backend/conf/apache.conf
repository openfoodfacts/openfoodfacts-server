# Apache 2.4 + mod_perl handles only the dynamic HTML generated pages
# and CGI scripts.
# Static files are served directly by the NGINX reverse proxy.

LoadModule perl_module modules/mod_perl.so

ServerAdmin contact@productopener.localhost

PerlSwitches -I/opt/product-opener/lib

PerlWarn On
PerlRequire /opt/product-opener/lib/startup_apache2.pl

# log the X-Forwarded-For IP address (the client ip) in access_log
LogFormat "%{X-Forwarded-For}i %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\"" proxy

<Location /cgi>
SetHandler perl-script
PerlResponseHandler ModPerl::Registry
PerlOptions +ParseHeaders
Options +ExecCGI
Require all granted
</Location>

<VirtualHost *>
DocumentRoot /opt/product-opener/html
ServerName productopener.localhost
LogLevel warn
ScriptAlias /cgi/ "/opt/product-opener/lib/cgi/"

<Directory /opt/product-opener/lib/html>
Require all granted
</Directory>

</VirtualHost>

PerlPostReadRequestHandler My::ProxyRemoteAddr 
