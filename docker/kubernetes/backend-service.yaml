apiVersion: v1
kind: Service
metadata:
  name: po-backend
  labels:
    app: po
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      protocol: TCP
      name: http
  selector:
    app: po
    component: backend
