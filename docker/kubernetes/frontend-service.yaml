apiVersion: v1
kind: Service
metadata:
  name: po-frontend
  labels:
    app: po
    component: frontend
spec:
  type: LoadBalancer
  ports:
    - port: 80
      protocol: TCP
      name: http
  selector:
    app: po
    component: frontend
